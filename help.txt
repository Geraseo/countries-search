// flag
      let flag_img = document.createElement("img");
      flag_img.setAttribute("id", "flag_img");
      tas_div.appendChild(flag_img);
      document.getElementById("flag_img").src = data[0].flags.png;

      // name
      let country_name = document.createElement("h2");
      country_name.setAttribute("id", "country_name");
      tas_div.appendChild(country_name);
      document.getElementById("country_name").innerHTML = data[0].name.common;

      // capital
      let capital = document.createElement("p");
      capital.setAttribute("id", "capital");
      tas_div.appendChild(capital);
      document.getElementById("capital").innerHTML = data[0].capital[0];

      //population
      let population = document.createElement("p");
      population.setAttribute("id", "population");
      tas_div.appendChild(population);
      document.getElementById(
        "population"
      ).innerHTML = `Population: ${data[0].population}`;